version: "0.1"

services:
  web:
    build: .
    ports:
      - "${PORT}:${PORT}"
    env_file: ".back.env"
    environment:
      HOST: "${HOST}"
      PORT: "${PORT}"
    develop:
      watch:
        - action: sync
          path: .
          target: ./
